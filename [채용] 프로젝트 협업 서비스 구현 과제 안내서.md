# [채용] 풀스택 개발자 채용 과제

**Project Collab — 프로젝트 협업 서비스 구현**

---

## 1. 개요

| 항목 | 내용 |
| :-- | :-- |
| 목표 | 프로젝트와 작업을 여러 사용자가 함께 관리하는 협업 서비스 구현 |
| 제출 기한 | 2026년 8월 26일(수) 23:59 |
| 제출 형식 | 안내 메일 회신으로 GitHub Repository(공개) 링크 전달 |

프로젝트마다 멤버의 역할이 다르고, 역할에 따라 할 수 있는 일이 다릅니다.

본 과제는 **백엔드를 중심으로 구성했습니다.** 프론트엔드는 선택 항목이며, 프론트엔드 역량은 면접에서 별도로 확인합니다.

완성하지 못해도 제출 가능합니다.

---

## 2. 도메인

| 도메인 | 설명 |
| :-- | :-- |
| 사용자 | 서비스를 이용하는 사람 |
| 프로젝트 | 사용자들이 함께 일하는 단위. 멤버와 역할을 가진다 |
| 작업 | 프로젝트에 속한 할 일. 제목 · 담당자 · 상태를 가진다 |

같은 사용자라도 프로젝트가 다르면 역할이 다를 수 있습니다.

작업의 **상태 종류와 그 외 속성은 지원자가 정합니다.**

---

## 3. 구현 기능 명세

### 사용자

- 사용자 등록 · 조회
- 인증은 구현 대상이 아닙니다. 요청자의 식별자는 **API 파라미터로 전달**된다고 가정합니다

### 프로젝트

- 프로젝트 생성 · 조회 · 수정 · 삭제
- **내가 속한 프로젝트 목록 조회**
- 프로젝트 멤버 추가 · 역할 변경 · 제거 · 목록 조회

### 작업

- 작업 생성 · 조회 · 수정 · 삭제
- 작업 목록 조회 — **검색, 상태 필터, 페이징**
- **두 사용자가 같은 작업을 동시에 수정하면, 나중 요청이 앞선 변경을 덮어씁니다.** 이 상황을 어떻게 다룰지 결정하고 구현하세요

엔드포인트 경로, HTTP 메서드, 파라미터·응답 스키마 등 **REST API 설계는 전적으로 지원자에게 맡깁니다.**

---

## 4. 권한 규칙

역할은 `OWNER` · `ADMIN` · `MEMBER` 세 가지입니다.

| 기능 | 수행 가능한 사람 |
| :-- | :-- |
| 프로젝트 생성 | 누구나 (생성자가 OWNER 멤버가 됩니다) |
| 프로젝트 삭제 | OWNER |
| 프로젝트 수정 | OWNER, ADMIN |
| 멤버 추가 · 역할 변경 · 제거 | OWNER, ADMIN |
| 프로젝트 상세 조회 · 멤버 목록 조회 | 프로젝트 멤버 전원 |
| 작업 생성 · 조회 · 목록 조회 | 프로젝트 멤버 전원 |
| 작업 수정 · 삭제 | 해당 작업의 담당자 본인, 또는 OWNER · ADMIN |

프로젝트에는 **항상 최소 1명의 OWNER가 있어야 합니다.**

프로젝트에 속하지 않은 사용자는 그 프로젝트와 프로젝트의 작업에 대해 **조회를 포함해 아무것도 할 수 없습니다.** 다른 프로젝트의 데이터가 목록이나 상세 응답에 섞여 나와서는 안 됩니다.

---

## 5. 기술 · 설계 제약

클론 후 `./gradlew bootRun` 한 번으로 바로 동작해야 합니다.

### 필수 스택

| 항목 | 내용 |
| :-- | :-- |
| 언어 · 프레임워크 | Java 17 · Spring Boot 3.3.x |
| 영속성 | JPA (Hibernate) |
| DB | H2 인메모리 |
| 빌드 | Gradle 8.x |
| 문서화 | OpenAPI 3 (Swagger UI)로 직접 설계한 API 노출 |

테스트는 JUnit 5를 권장하며, 작성 여부는 지원자가 판단합니다.

### 지원자가 결정하는 것

- 테이블 설계
- REST API 설계 (URI · HTTP 메서드 · 요청·응답 형식 · 오류 응답 형식)
- 패키지 구조와 계층 구성

### 그 외 기술 사용

QueryDSL, MapStruct, Redis, MongoDB, Kafka, Docker, CI 등 무엇이든 자유롭게 사용해도 좋습니다. 다만 **왜 그것이 필요했는지를 README에 적어 주세요.**

**과하거나 의미 없는 설계는 이점이 아닙니다.** 규모에 맞지 않는 구조와 기술은 오히려 감점입니다. 쓰지 않기로 한 판단도 똑같이 좋은 답입니다.

---

## 6. 프론트엔드 (선택)

여력이 되면 위 API를 사용하는 화면을 만들어 주세요. **React**로 구현합니다.

- 범위는 자유입니다. 작업 목록 화면 하나만 있어도 충분합니다
- 필수가 아닙니다. **백엔드가 우선입니다**
- 하지 않아도 감점하지 않습니다. 프론트엔드 역량은 면접에서 확인합니다

---

## 7. 제출물 체크리스트 (README.md)

- 빌드 & 실행 방법, **Swagger UI 접속 경로**, 그리고 실행 직후 기능을 확인하는 절차 (초기 데이터를 넣어두면 좋습니다)
- 설계한 REST API 명세 요약 (엔드포인트 · 파라미터 · 응답 예시)
- **주요 설계 결정과 그 이유**
- **사용한 기술과 그것을 선택한 이유**
- **여러 회사가 이 서비스를 함께 쓰고 회사끼리 데이터가 완전히 분리되어야 한다면, 어디를 어떻게 바꾸겠는지**
- 구현하지 못한 부분과, 시간이 더 있었다면 어떻게 진행했을지

---

## 8. 기타 안내

- 이 과제는 주어진 설계를 그대로 구현하는 것이 아니라, **명세에 없는 부분을 스스로 판단해 채우는 것까지 포함합니다.** 정해지지 않았거나 헷갈리는 부분은 본인이 결정하고, 그 근거를 README에 남겨 주세요.
- 과제 관련한 **별도 질의응답은 제공되지 않습니다.** 완성도를 평가하기보다는 문제 접근 방식과 설계 의도, 코드 품질을 중심으로 봅니다.
- **AI 도구(Claude, ChatGPT, Copilot 등) 사용을 권장합니다.** 저희도 실무에서 사용합니다. 다만 제출한 코드는 전부 본인이 설명할 수 있어야 합니다. 면접에서 **왜 그렇게 설계했는지, 다른 선택지는 무엇이었는지**를 묻습니다.
- **완벽히 마무리하지 못해도 현재 상태 그대로 제출해 주세요.** README 설명을 통해 진행 과정을 충분히 확인합니다.
- API 키, 비밀번호 등 민감한 정보는 저장소에 포함하지 말아 주세요.
